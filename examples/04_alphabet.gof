// Prints all characters of the alphabet.

define ALPHABET_SIZE 26 end

0 while copy ALPHABET_SIZE < do
    // From to 0 to alphabet size.

    // Write ['a'+I] to [I].
    copy MPTR + copy 'a' + mwrite
inc end free

// Show characters.
MPTR ALPHABET_SIZE mshowc